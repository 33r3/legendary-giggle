DATABASE_URL=sqlite:///./data/raw.db
INGEST_WEBHOOK_TOKEN=changeme
# Drives content/economy generation. Set a real, unpublished value in
# production; leave the default for local dev.
GAME_SEED=dev-local
# HTTP Basic auth for the web dashboard (/). Set a real password in
# production — this is reachable on the same public domain as ingest.
WEB_UI_USERNAME=player
WEB_UI_PASSWORD=changeme
